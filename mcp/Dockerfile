# Minimal image for Glama and other MCP inspectors: installs the published
# npm package and starts the stdio server. No build context needed.
FROM node:20-alpine
RUN npm install -g lotero-mcp
CMD ["lotero-mcp"]
