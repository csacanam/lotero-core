# Template for NextJS environment variables.
# Copy this file, rename it to .env.local for local dev, or configure in Vercel for production.

# Defaults exist in scaffold.config.ts for prototyping, but use your own keys for production.

# -----------------------------------------------------------------------------
# Wallet & RPC
# -----------------------------------------------------------------------------
NEXT_PUBLIC_ALCHEMY_API_KEY=      # Get one at https://alchemy.com
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=  # Get one at https://cloud.walletconnect.com

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_APP_URL=            # Base URL for referral links (default: http://localhost:3000)
