# Template for Hardhat environment variables.
# Copy this file, rename it to .env, and fill in the values.

# Defaults exist in hardhat.config.ts for local prototyping, but use your own keys for production.

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
ALCHEMY_API_KEY=          # Get one at https://alchemy.com
DEPLOYER_PRIVATE_KEY=     # Your wallet private key (never commit!). Run `yarn generate` to create one.

# -----------------------------------------------------------------------------
# Optional (for contract verification)
# -----------------------------------------------------------------------------
ETHERSCAN_API_KEY=        # Same key works for Etherscan, Basescan, Arbiscan, etc.

# -----------------------------------------------------------------------------
# Optional (only for mainnet/testnet deploys)
# -----------------------------------------------------------------------------
# Create a subscription at https://vrf.chain.link, fund it with LINK, then set:
BASE_VRF_SUBSCRIPTION_ID=         # Required for `yarn deploy --network base`
ARBITRUM_VRF_SUBSCRIPTION_ID=     # Required for `yarn deploy --network arbitrum`

# -----------------------------------------------------------------------------
# Optional (for forking mainnet locally)
# -----------------------------------------------------------------------------
# MAINNET_FORKING_ENABLED=true   # Set when running `yarn fork`
